import '../interview_category_constants.dart';
import '../models/interview_question.dart';

InterviewQuestion _react(int n, String q, String a,
        {String frequency = 'High', String? code, List<String> tags = const []}) =>
    InterviewQuestion(
      id: 'react-${n.toString().padLeft(2, '0')}',
      categoryId: InterviewCategories.react,
      question: q,
      detailedAnswer: a,
      frequency: frequency,
      tags: tags,
      codeExample: code,
    );

final List<InterviewQuestion> reactInterviewQuestions = [
  _react(1, 'What is Virtual DOM and how does it work?',
      'React keeps a lightweight in-memory tree of UI. On state change, React builds a new virtual tree, diffs it with the previous one (reconciliation), and applies minimal updates to the real DOM. This batches changes for performance and gives a declarative programming model.',
      frequency: 'Every interview', tags: ['core']),
  _react(2, 'Explain React component lifecycle (hooks equivalent)',
      'Class lifecycle: mount (constructor, render, componentDidMount), update (render, componentDidUpdate), unmount (componentWillUnmount). Hooks mapping: useState/useReducer for state, useEffect for mount/update/unmount side effects, useLayoutEffect before paint. Strict Mode double-invokes effects in dev to surface bugs.',
      frequency: 'Every interview', tags: ['core']),
  _react(3, 'useState vs useReducer — when to use which?',
      'useState for simple independent values. useReducer when state logic is complex, multiple sub-values update together, or next state depends on previous with structured actions. Reducer pattern mirrors Redux and is easier to test.',
      frequency: 'Very High', tags: ['hooks'],
      code: 'const [state, dispatch] = useReducer(reducer, initialState);'),
  _react(4, 'Explain useEffect — dependency array rules',
      'useEffect runs after paint. [] runs once on mount (and cleanup on unmount). [dep] re-runs when dep changes. Omitting deps re-runs every render (usually a bug). Cleanup function runs before re-run and on unmount. Do not put non-stable objects/functions without memoization.',
      frequency: 'Every interview', tags: ['hooks']),
  _react(5, 'What causes unnecessary re-renders? How to fix?',
      'Parent re-render, new object/function props each render, context changes, state updates high in tree. Fixes: React.memo, useMemo/useCallback, split context, colocate state, key stability, virtualization for long lists.',
      frequency: 'Very High', tags: ['performance']),
  _react(6, 'Explain React.memo, useMemo, useCallback',
      'React.memo skips re-render if props shallow-equal. useMemo caches computed values. useCallback caches function references. Overuse adds complexity — profile first. Essential when passing callbacks/objects to memoized children.',
      frequency: 'Very High', tags: ['performance']),
  _react(7, 'What is Context API? When is it NOT enough?',
      'Context shares values without prop drilling. Good for theme, locale, auth session. Not ideal for high-frequency updates or large app state — causes broad re-renders. Use Redux/Zustand/React Query for complex server/cache state.',
      frequency: 'High', tags: ['state']),
  _react(8, 'Controlled vs Uncontrolled components',
      'Controlled: React state is source of truth (value + onChange). Uncontrolled: DOM holds state via ref. Controlled enables validation and single source of truth; uncontrolled is simpler for one-off forms. React Hook Form can mix both.',
      frequency: 'High', tags: ['forms']),
  _react(9, 'What are keys in React? Why are they important?',
      'Keys help reconciliation identify list items across renders. Stable unique keys preserve component state and avoid bugs. Never use array index as key when list reorders/filters/inserts — causes wrong state attachment.',
      frequency: 'High', tags: ['core']),
  _react(10, 'Explain error boundaries',
      'Class components (or libraries) catching render errors in children, showing fallback UI. They do not catch event handler errors, async, or SSR errors. React 19+ moves toward more resilient patterns; still know the concept for legacy codebases.',
      frequency: 'Medium', tags: ['error handling']),
  _react(11, 'How does React Router work?',
      'Client-side router maps URL to components without full page reload. Browser history API (pushState). Nested routes, layouts, loaders (v6.4+), and protected routes via wrappers. Mention code splitting per route with lazy().',
      frequency: 'High', tags: ['routing']),
  _react(12, 'What are portals? When to use them?',
      'ReactDOM.createPortal renders children into a different DOM node while keeping React tree/event bubbling. Use for modals, tooltips, dropdowns escaping overflow:hidden parents. Accessibility: focus trap and aria-modal.',
      frequency: 'Medium', tags: ['advanced']),
  _react(13, "Explain React's reconciliation algorithm",
      'Diff two virtual trees level by level. Same type: update props/DOM. Different type: tear down and rebuild subtree. Keys optimize list diffing. Fiber architecture enables interruptible rendering and priorities (React 18 concurrent features).',
      frequency: 'High', tags: ['core']),
  _react(14, 'What is prop drilling? Solutions?',
      'Passing props through many intermediate layers that do not use them. Solutions: Context, composition (children slot), state libraries, or colocate state closer to consumers. Choose based on update frequency and team patterns.',
      frequency: 'High', tags: ['state']),
  _react(15, 'Server components vs Client components (Next.js)',
      'Server Components render on server, zero JS shipped for their logic, fetch data close to source. Client Components ("use client") handle interactivity, hooks, browser APIs. App Router composes both for performance.',
      frequency: 'Growing', tags: ['next.js']),
  _react(16, 'What are custom hooks? Build one live',
      'Functions starting with use that compose built-in hooks. Extract reusable stateful logic (useFetch, useDebounce, useLocalStorage). Rules of Hooks apply. Example: useDebounce wraps useState + useEffect timer.',
      frequency: 'Very High', tags: ['hooks'],
      code: 'function useDebounce(value, ms) {\n  const [v, setV] = useState(value);\n  useEffect(() => {\n    const t = setTimeout(() => setV(value), ms);\n    return () => clearTimeout(t);\n  }, [value, ms]);\n  return v;\n}'),
  _react(17, 'Explain code splitting and lazy loading',
      'Split bundle into chunks loaded on demand via dynamic import() and React.lazy + Suspense. Reduces initial load. Route-based splitting is most common. Provide fallback UI and error boundaries for failed chunk loads.',
      frequency: 'Medium', tags: ['performance'],
      code: 'const Page = lazy(() => import("./Page"));\n<Suspense fallback={<Spinner />}><Page /></Suspense>'),
  _react(18, 'How do you handle forms in React?',
      'Controlled inputs with validation state, or libraries (React Hook Form, Formik). Validate on blur/submit, show accessible errors, prevent double submit. For server actions (Next.js), progressive enhancement without full client JS.',
      frequency: 'High', tags: ['forms']),
  _react(19, 'What is state colocation?',
      'Keep state as close as possible to where it is used. Lifting state only when siblings need to share. Reduces re-render scope and simplifies reasoning. Opposite of premature global state.',
      frequency: 'Medium', tags: ['state']),
  _react(20, 'Explain the render → commit phase',
      'Render phase: React calls components, diffs fiber tree (can be interrupted in concurrent mode). Commit phase: applies DOM updates, runs layout effects, then passive effects. useLayoutEffect runs before paint; useEffect after paint.',
      frequency: 'Medium', tags: ['core']),
];
