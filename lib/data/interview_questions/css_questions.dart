import '../interview_category_constants.dart';
import '../models/interview_question.dart';

InterviewQuestion _css(int n, String q, String a,
        {String frequency = 'High', String? code, List<String> tags = const []}) =>
    InterviewQuestion(
      id: 'css-${n.toString().padLeft(2, '0')}',
      categoryId: InterviewCategories.css,
      question: q,
      detailedAnswer: a,
      frequency: frequency,
      tags: tags,
      codeExample: code,
    );

final List<InterviewQuestion> cssInterviewQuestions = [
  _css(1, 'Explain the CSS Box Model',
      'Every element is a rectangular box: content → padding → border → margin. width/height by default apply to content box. box-sizing: border-box includes padding and border in width — preferred for layouts.',
      frequency: 'Very High', tags: ['layout'],
      code: '* { box-sizing: border-box; }'),
  _css(2, 'Flexbox vs Grid — when to use which?',
      'Flexbox: one-dimensional layouts (row OR column) — nav bars, toolbars, aligning items. Grid: two-dimensional rows AND columns — page layouts, dashboards. Often combine: grid for page, flex inside cells.',
      frequency: 'Very High', tags: ['layout']),
  _css(3, 'What is CSS specificity? How is it calculated?',
      'Specificity determines which rule wins: inline (1000), IDs (100), classes/attributes/pseudo-classes (10), elements (1). !important overrides normal cascade. Equal specificity: last rule wins. Avoid specificity wars — use consistent methodology (BEM).',
      frequency: 'High', tags: ['cascade']),
  _css(4, 'Position values — static, relative, absolute, fixed, sticky',
      'static: default flow. relative: offset from self, preserves space. absolute: removed from flow, positioned vs nearest positioned ancestor. fixed: vs viewport. sticky: hybrid — relative until scroll threshold then fixed-like.',
      frequency: 'High', tags: ['layout']),
  _css(5, 'How does z-index work? What is stacking context?',
      'z-index orders positioned elements within the same stacking context. New contexts from transform, opacity < 1, filter, isolation, etc. Children cannot escape parent context — common modal/overlay bug source.',
      frequency: 'High', tags: ['layout']),
  _css(6, 'What is BEM? Why use it?',
      'Block__Element--Modifier naming: card, card__title, card--featured. Explicit structure, low specificity, scalable teams. Alternatives: CSS Modules, utility-first (Tailwind). Pick one system and stay consistent.',
      frequency: 'Medium', tags: ['architecture']),
  _css(7, 'How do you make a website responsive?',
      'Fluid layouts, relative units (%, rem, vw), media queries, flexible images (max-width: 100%), mobile-first breakpoints, container queries for component-level responsiveness. Test real devices and touch targets (44px min).',
      frequency: 'Very High', tags: ['responsive']),
  _css(8, 'Difference between em, rem, px, %, vw/vh',
      'px: absolute pixels. em: relative to element font-size (compounds). rem: root font-size (predictable). %: parent-dependent. vw/vh: viewport fractions. Prefer rem for spacing/type scale; vw/vh for full-bleed sections.',
      frequency: 'High', tags: ['units']),
  _css(9, 'How do CSS animations work? Transition vs Animation?',
      'transition: property change over time (hover states). @keyframes animation: multi-step, repeatable, independent of property change. Use transform/opacity for GPU-friendly 60fps. Respect prefers-reduced-motion.',
      frequency: 'Medium', tags: ['animation']),
  _css(10, 'What is a pseudo-element? Give examples',
      'Pseudo-elements style specific parts: ::before, ::after (decorative content), ::placeholder, ::selection. Require content property on ::before/::after. Different from pseudo-classes (:hover) which style element states.',
      frequency: 'Medium', tags: ['selectors']),
  _css(11, 'How to center a div? (horizontal + vertical)',
      'Flex parent: display:flex; justify-content:center; align-items:center. Grid: place-items:center. Absolute + transform: top/left 50% + translate(-50%,-50%). Modern: margin:auto in grid/flex child. Know trade-offs for unknown dimensions.',
      frequency: 'Every interview', tags: ['layout'],
      code: '.parent { display:flex; justify-content:center; align-items:center; min-height:100vh; }'),
  _css(12, 'What is CSS-in-JS? Pros and cons?',
      'Styles colocated with components (styled-components, Emotion). Pros: scoped styles, dynamic theming, co-location. Cons: runtime cost, SSR complexity, larger bundles. Alternatives: CSS Modules, Tailwind, vanilla-extract (zero-runtime).',
      frequency: 'Medium', tags: ['architecture']),
  _css(13, 'Explain mobile-first vs desktop-first approach',
      'Mobile-first: base styles for small screens, min-width media queries add complexity up. Desktop-first: max-width queries simplify down. Mobile-first is industry default — performance and progressive enhancement.',
      frequency: 'High', tags: ['responsive']),
  _css(14, 'What are CSS variables? How are they scoped?',
      'Custom properties (--primary: #0af) inherit in the cascade. Set on :root for global theme; override locally. JS can read/write via getComputedStyle/setProperty. Great for theming and design tokens.',
      frequency: 'Medium', tags: ['modern css'],
      code: ':root { --gap: 1rem; }\n.card { padding: var(--gap); }'),
  _css(15, 'How does the cascade work in CSS?',
      'Origin (user agent < author < user !important), importance (!important), specificity, source order. Later rules win at equal specificity. @layer (modern) controls explicit cascade layers. Understanding cascade debugs "why is my style ignored".',
      frequency: 'High', tags: ['core']),
];
